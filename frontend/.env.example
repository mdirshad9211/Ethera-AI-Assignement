# API base URL (no trailing slash).
# Leave empty in development to use the Vite proxy to the backend (see vite.config.js).
VITE_API_URL=

# Or point at your API explicitly (also used in production builds):
# VITE_API_URL=http://localhost:4000
